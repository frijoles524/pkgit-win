local base_dir = home_dir .. "/pkgit"

install_directories = {
    bin      = base_dir .. "/bin",
    lib      = base_dir .. "/lib",
    include  = base_dir .. "/include",
    pkgblds  = base_dir .. "/src"
}
